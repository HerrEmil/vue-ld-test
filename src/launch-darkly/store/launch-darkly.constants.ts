export const ldModuleName = 'launchDarkly';

export enum ELaunchDarklyMutation {
  SetFlags = 'setFlags',
  SetReady = 'setReady',
  SetError = 'setError',
}

export enum ELaunchDarklyAction {
  SetFlags = 'setFlags',
  SetReady = 'setReady',
  SetError = 'setError',
}
